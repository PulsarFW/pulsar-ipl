name: Create release

on:
  push:
    tags:
      - 'v*.*.*'

permissions:
  contents: write

jobs:
  create-release:
    runs-on: ubuntu-latest
    steps:
      - name: Get latest code
        uses: actions/checkout@v4

      - name: Bundle files
        run: |
          mkdir -p ./temp/pulsar-ipl
          rsync -a --exclude='.github/' . ./temp/pulsar-ipl/
          cd ./temp && zip -r ../pulsar-ipl.zip ./pulsar-ipl

      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          name: "PULSAR-IPL ${{ github.ref_name }}"
          body: |
            ![Pulsar Framework](https://r2.fivemanage.com/GPYOH8Hq4GPyAY7czrgLe/pulsarbanner.png)

            ## PULSAR-IPL ${{ github.ref_name }}

            GTA Online DLC interior and IPL loader for Pulsar Framework.

            ### Installation
            1. Download `pulsar-ipl.zip` below
            2. Extract into your server's `resources` folder
            3. Add `ensure pulsar-ipl` to your `server.cfg`

            ---
            ![Pulsar Framework](https://img.shields.io/badge/Pulsar-Framework-7c3aed?style=flat-square)
            ![Built for FiveM](https://img.shields.io/badge/Built_for-FiveM-F40552?style=flat-square)
          files: pulsar-ipl.zip
          prerelease: false
